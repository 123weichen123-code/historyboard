# 启动说明（首版发布包）

## 1. 环境要求

- Java 17+（需保证 `java -version` 可运行）
- Maven 3.9+（需保证 `mvn -version` 可运行）

## 2. 一键联调启动

```bash
cd /Users/mercury/Documents/ChronoAtlas-HistoryBoard-JavaH5-20260321
./scripts/dev-java.sh
```

如 8080 端口被占用，可改端口后启动：

```bash
cd /Users/mercury/Documents/ChronoAtlas-HistoryBoard-JavaH5-20260321
HB_PORT=18080 ./scripts/dev-java.sh
```

启动成功后访问：

- http://127.0.0.1:18080
- Swagger UI: http://127.0.0.1:18080/swagger-ui.html

## 3. 本次自检结果

- 自检入口：`./scripts/dev-java.sh`
- 自检时间：2026-03-21（America/Los_Angeles）
- 结果：成功
- 健康检查：`GET /api/health` 返回 `{"status":"ok"}`
- 详细日志：`selfcheck/dev-java-selfcheck.log`
- 元信息：`selfcheck/dev-java-selfcheck.meta`

## 4. OpenAPI 导出

发布包内已附带：

- `openapi/historyboard.openapi.yaml`（YAML 规范）
- `openapi/historyboard.openapi.export.json`（运行中服务导出 JSON）
